# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'FormUpload' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for FormUpload
  pod 'SwiftyDropbox'

  target 'FormUploadTests' do
    inherit! :search_paths
    # Pods for testing
	pod 'SwiftyDropbox'
  end

end
